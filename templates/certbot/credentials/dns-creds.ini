
{% if 'LETSENCRYPT_AUTHENTICATOR' in env and env['LETSENCRYPT_AUTHENTICATOR'] == "cloudflare" %}
# Cloudflare API token used by Certbot
dns_cloudflare_api_token = {{ env['LETSENCRYPT_DNS_KEY'] }}
{% elif 'LETSENCRYPT_AUTHENTICATOR' in env and env['LETSENCRYPT_AUTHENTICATOR'] == 'linode' %}
# Linode API credentials used by Certbot
dns_linode_key = {{ env['LETSENCRYPT_DNS_KEY'] }}
# dns_linode_version = [<blank>|3|4]
{% else %}
# Leaving it blank as is no credentials template defined for {{ active_authenticator }}. 
{% endif %}
